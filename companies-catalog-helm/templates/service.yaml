apiVersion: v1
kind: Service
metadata:
  name: {{ include "companies-catalog-helm.fullname" . }}
  labels:
    {{- include "companies-catalog-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "companies-catalog-helm.selectorLabels" . | nindent 4 }}
