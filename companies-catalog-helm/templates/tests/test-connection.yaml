apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "companies-catalog-helm.fullname" . }}-test-connection"
  labels:
    {{- include "companies-catalog-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "companies-catalog-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
